# Firebase Notes

Firebase is useful for rapid prototype interaction:

- Realtime Database for vote bars and live HUD values
- Firestore for prompt records and Render Vault entries
- Functions for Colorway DNA Alignment scoring
- Auth for host/admin roles

See `firestore.schema.json` for a starting schema.
